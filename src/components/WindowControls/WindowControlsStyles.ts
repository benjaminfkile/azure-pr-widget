import { makeStyles} from "@fluentui/react-components";

const windowControlsStyles = makeStyles({
  root: {
    display: "flex",
    alignItems: "center",
    height: "100%",
  },
});

export default windowControlsStyles;
